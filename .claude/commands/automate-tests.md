---
description: Generate C# Playwright/NUnit automation from a manual test case file
---
Use the `playwright-automation-engineer` agent to generate Playwright/NUnit automation code for the manual test case file below, following the `playwright-automation-generator` skill and the project `CLAUDE.md` guidelines. Build and run the tests before reporting completion.

Test case file (path under `TestCases/`, or describe the feature if the file doesn't exist yet):
$ARGUMENTS
