---
description: Turn a requirement into manual QA test cases (single Markdown file under TestCases/)
---

Use the `qa-test-designer` agent to produce manual QA test cases for the following requirement. Follow the `manual-test-case-writer` skill and the project `CLAUDE.md` guidelines. Output a single file under `TestCases/`, and end with the handoff line pointing to `/automate-tests`.

Requirement:
$ARGUMENTS
